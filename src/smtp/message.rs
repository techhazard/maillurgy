// TODO: finish
/// actual email and its data
pub struct Message {
    Headers: Vec<String>,
    ToAddress: Vec<String>,
    CcAddress: Vec<String>,
    BccAddress: Vec<String>,
    Body: Vec<String>
}
